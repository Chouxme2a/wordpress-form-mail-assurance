{
  "name": "wordpress-form-mail-assurance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "proof:real": "node scripts/run-real-proof.js",
    "proof:cycle": "node scripts/run-real-cycle.js",
    "plugin:zip": "cd wordpress-plugin && zip -qr ../public/wp-form-mail-assurance.zip wp-form-mail-assurance",
    "deploy:worker": "wrangler deploy -c worker/wrangler.toml",
    "deploy:pages": "wrangler pages deploy public --project-name wp-form-mail-assurance",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  },
  "engines": {
    "node": ">=20"
  }
}
